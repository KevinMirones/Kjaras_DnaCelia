 <!-- ══ GALERÍA ════════════════════════════════════════════════════ -->
  <section id="galeria" class="section" aria-labelledby="galeria-title">
    <div class="container">
      <header class="section-header reveal">
        <span class="section-label">Nuestros momentos</span>
        <h2 class="section-title" id="galeria-title">La <em>Galería</em></h2>
        <div class="section-rule"></div>
      </header>

      <!-- Gallery grid (populated by JS) -->
      <div id="galeria-grid" class="gallery-grid" role="list"></div>
    </div>

  </section>

  <!-- Lightbox -->
  <div id="lightbox" class="lightbox" role="dialog" aria-label="Imagen ampliada" aria-modal="true">
    <div class="lightbox-inner">
      <button id="lightbox-close" class="lightbox-close" aria-label="Cerrar">✕</button>
      <img id="lightbox-img" class="lightbox-img" src="" alt="" />
    </div>
  </div>
